/**
 * Web is pinned to light mode for now so GitHub Pages renders consistently
 * regardless of the browser or device preferred color scheme.
 */
export function useColorScheme() {
  return 'light';
}
